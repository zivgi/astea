import { DirectiveExample1Directive } from './directive-example1.directive';

describe('DirectiveExample1Directive', () => {
  it('should create an instance', () => {
    const directive = new DirectiveExample1Directive();
    expect(directive).toBeTruthy();
  });
});
